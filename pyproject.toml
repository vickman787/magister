[project]
name = "magister"
version = "0.1.0"
description = "Magister - risk-governed portfolio agent for the Binance Agent OS Mini Hackathon (Track A)"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.40",
    "pydantic>=2.7",
    "PyYAML>=6.0",
    "requests>=2.31",
    "python-dotenv>=1.0",
    "mcp>=1.2",
]

[tool.uv]
package = false
